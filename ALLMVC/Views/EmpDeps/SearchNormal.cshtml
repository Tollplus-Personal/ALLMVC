@model IEnumerable<ALLMVC.Models.EmpDep>

@{
    ViewData["Title"] = "Search";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@*<form method="get" asp-action="SearchNormal">
        <div class="form-group">
            <div class="input-group">
                <input type="search" class="form-control"
                    name="Search" value="@ViewData["GetEmpDep"]"/>
                <span class="input-group-btn">
                    <button class="btn btn-default">
                        <i class="btn btn-danger"></i>
                    </button>
                </span>
            </div>
        </div>
    </form>*@
<form method="get" asp-action="SearchNormal">
    <p>
        <input type="search" name="Search" value="@ViewData["GetEmpDep"]" style="width:500px;" />
        <input type="submit" value="Search" class="btn btn-danger" />
        <a asp-action="Index">Show all elements</a>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmpName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepartmentId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmpName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DepartmentId)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
